#### 1) What is the difference between `null` and `undefined`?

Ans:
undefine মানে variable declared করা হয়েছে কিন্তু value assigne করা হয়নি।

null হল কোন object এর value না থাকলে তখন এইটা use করা হয়

#### 2) What is the use of the `map()` function in JavaScript? How is it different from `forEach()`?

Ans:
যখন কোনো কিছু return করতে হয় তখন map() use করা হয় ।

যখন কোনো কিছু return করতে হয় না তখন map() use করা হয় ।

#### 3) What is the difference between `==` and `===`?

Ans:
== data type check করে না শুধু value check করে

=== data type সহ সব check করে না শুধু value check করে

#### 4) What is the significance of `async`/`await` in fetching API data?

#### 5) Explain the concept of Scope in JavaScript (Global, Function, Block).
